- If blocked promotion recovery still proves uneven, extend the same structured failure metadata pattern beyond `warcraft_plan_approve` to other promotion blockers (for example lightweight-validation failures in `warcraft_task_expand` or `warcraft_plan_write`) so agents can recover exact next-step args from failures consistently.
