- If returning `taskExpandArgs` + `planApproveArgs` + `taskSyncArgs` still leaves too much orchestration overhead, add a higher-level promotion bundle or helper that chains those calls more explicitly (while still preserving the reviewed approval step) instead of only handing back individual arg payloads.
