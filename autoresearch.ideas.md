- Continue extending structured failure metadata to the remaining promotion/tooling blockers that still only return prose (for example plan-scaffold discovery failures or other task/plan validation errors), but only where agents genuinely need exact retry args or machine-readable blocker details.
